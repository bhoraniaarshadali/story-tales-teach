// This file stays the same but now imports from the llm-wrapper
import { generateStoryWithLLM } from "./llm-wrapper.ts";

// Export the function so it can be used by other files
export { generateStoryWithLLM };
